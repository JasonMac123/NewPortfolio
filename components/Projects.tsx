"use client";

import SectionTitle from "./SectionTitle";

const Projects = () => {
  return (
    <section>
      <SectionTitle>My Projects</SectionTitle>
    </section>
  );
};

export default Projects;
